"use strict";Object.defineProperty(exports, "__esModule", {value: true}); function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { newObj[key] = obj[key]; } } } newObj.default = obj; return newObj; } }// src/index.ts
var _tools = require('@bare-ts/tools');
var _promises = require('fs/promises'); var fs = _interopRequireWildcard(_promises);
var _path = require('path'); var path = _interopRequireWildcard(_path);

async function compileSchema(options) {
  const { schemaPath, outputPath, config = {} } = options;
  let schema = await fs.readFile(schemaPath, "utf-8");
  schema = schema.split(/\r?\n/).filter((line) => !line.trimStart().startsWith("//")).join("\n");
  schema = schema.replace(/map<([^,>]+),\s*([^>]+)>/g, "map<$1><$2>");
  const toCamel = (name) => name.replace(/_([a-zA-Z0-9])/g, (_, c) => c.toUpperCase());
  schema = schema.replace(
    /^(\s*)([a-z][a-z0-9_]*)(\s*:\s*)/gim,
    (_m, p1, p2, p3) => {
      return `${p1}${toCamel(p2)}${p3}`;
    }
  );
  const outputDir = path.dirname(outputPath);
  await fs.mkdir(outputDir, { recursive: true });
  const defaultConfig = {
    pedantic: true,
    generator: "ts",
    ...config
  };
  let result = _tools.transform.call(void 0, schema, defaultConfig);
  result = postProcessAssert(result);
  await fs.writeFile(outputPath, result);
}
var ASSERT_FUNCTION = `
function assert(condition: boolean, message?: string): asserts condition {
    if (!condition) throw new Error(message ?? "Assertion failed")
}

`;
function postProcessAssert(code) {
  code = code.replace(/^import assert from "node:assert\/strict"/m, "");
  code += "\n" + ASSERT_FUNCTION;
  return code;
}




exports.compileSchema = compileSchema; exports.transform = _tools.transform;
//# sourceMappingURL=chunk-NSGEDZKC.js.map