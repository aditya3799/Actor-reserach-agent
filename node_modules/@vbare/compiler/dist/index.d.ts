import { Config } from '@bare-ts/tools';
export { Config, transform } from '@bare-ts/tools';

interface CompileOptions {
    schemaPath: string;
    outputPath: string;
    config?: Partial<Config>;
}
declare function compileSchema(options: CompileOptions): Promise<void>;

export { type CompileOptions, compileSchema };
